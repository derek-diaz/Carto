services:
  carto:
    build:
      context: .
      dockerfile: Dockerfile
    image: carto:latest
    container_name: carto
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
